# TODO

- [ ] Extract the GitHub Pages skill family (`create-github-pages`, `improve-github-pages`, `github-pages-content-architecture`, `github-pages-visual-system`, and `audit-github-pages-ux`) into a standalone repository distributed as its own plugin package. Preserve canonical sources, generated Claude/Codex copies, activation tests, release tooling, and third-party attribution during the move.
