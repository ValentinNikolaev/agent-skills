interface:
  display_name: "Code Review"
  short_description: "Review changes with actionable test gaps"
  default_prompt: "Use $code-review to inspect these changes, report confirmed findings and missing test scenarios, and defer costly optional checks until I approve them."
